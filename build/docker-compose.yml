version: '3'

services:
  spryker-sdk:
    image: spryker/php-sdk:0.1.2
    build:
      context: ./
      dockerfile: ./infrastructure/sdk.Dockerfile
    working_dir: "/project"
    volumes:
      - $PWD:/project:rw
#      - ./db:/data/db:rw
#      - ./extension:/data/extension:rw

volumes:
  project:
