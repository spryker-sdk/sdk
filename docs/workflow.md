### Workflow SDK tool

```bash
  spryker-sdk workflow:dump {workflowName}  | dot -Tsvg -o graph.svg // Generate image for concrete workflow
  spryker-sdk init:sdk:project --workflow={workflowName} // Init project settings with workflow
```
